import { Router } from 'express';
// TODO: import { register, login, getMe } from '../controllers/auth.js'
// TODO: import authenticate from '../middleware/authenticate.js'

const router = Router();

// TODO: POST /api/auth/register — create user, hash password, return JWT
// router.post('/register', register);

// TODO: POST /api/auth/login — verify credentials, return JWT
// router.post('/login', login);

// TODO: GET /api/auth/me — return current user from token (protected)
// router.get('/me', authenticate, getMe);

export default router;