import { Router } from 'express';
// TODO: import { startSession, endSession, getUserSessions } from '../controllers/sessions.js'
// TODO: import authenticate from '../middleware/authenticate.js'

const router = Router();

// TODO: router.use(authenticate);

// TODO: POST /api/sessions/start — log the start of a focus session
// router.post('/start', startSession);

// TODO: POST /api/sessions/end — mark session as completed, store duration
// router.post('/end', endSession);

// TODO: GET /api/sessions — return all sessions for the logged-in user
// router.get('/', getUserSessions);

export default router;