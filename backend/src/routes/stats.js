import { Router } from 'express';
// TODO: import { getStats } from '../controllers/stats.js'
// TODO: import authenticate from '../middleware/authenticate.js'

const router = Router();

// TODO: router.use(authenticate);

// TODO: GET /api/stats — return aggregated stats for the logged-in user:
//   - total focus time (all time)
//   - sessions per day for the past 7 days
//   - current streak (consecutive days with at least one session)
// router.get('/', getStats);

export default router;